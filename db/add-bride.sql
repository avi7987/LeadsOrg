-- =====================================================================
--  add-bride.sql — סימון "כלה" ללידים (מטרת-על)
--
--  מוסיף שדה is_bride. הזיהוי אוטומטי מההתכתבות (המילים כלה/מתחתנת/חתונה),
--  וניתן גם לסמן/לבטל ידנית בכרטיס הליד. משמש למדידה נפרדת של עולם הכלות.
--
--  איך מריצים (פעם אחת): Supabase → SQL Editor → הדבקה → Run.
-- =====================================================================

alter table leads add column if not exists is_bride boolean default false;
