{
  "name": "aya-leads-worker",
  "version": "1.0.0",
  "description": "WhatsApp lead-detection worker for Aya — listens on her existing number via a linked device (whatsapp-web.js) and writes leads to Supabase.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.23.0"
  }
}
